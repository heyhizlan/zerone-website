{
  "name": "zerone-website",
  "version": "1.0.0",
  "private": true,
  "description": "ZerOne — performance suspension catalogue. Static site, zero runtime dependencies.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --serve",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  },
  "dependencies": {
    "@fontsource-variable/figtree": "^5.3.0",
    "@fontsource-variable/saira": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@fontsource/michroma": "^5.3.0"
  }
}
